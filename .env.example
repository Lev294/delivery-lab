# Example placeholders only. scripts/init_env.py generates local passwords.
APP_PORT=8080
POSTGRES_PASSWORD=replace-with-generated-local-admin-password
APP_DB_PASSWORD=replace-with-generated-local-app-password
REPLICATION_PASSWORD=replace-with-generated-local-replication-password
# Set to 3 for the delayed gRPC reply experiment, then restore to 0.
INVENTORY_DELAY_SECONDS=0
